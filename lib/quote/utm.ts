/** Parámetros de campaña que se guardan con la solicitud (el resto se descarta). */
export const UTM_KEYS = ["utm_source", "utm_medium", "utm_campaign", "utm_term", "utm_content", "gclid", "fbclid"] as const;
